services:
  mcp-server:
    build: .
    ports:
      - "8001:8001"
    environment:
      - LOG_LEVEL=INFO
      - FASTMCP_HOST=0.0.0.0
      - FASTMCP_PORT=8001
    volumes:
      # Optional: Mount source code for development
      - ./src:/app/src
    restart: unless-stopped
